services:
  mailguardian:
    build: .
    container_name: mailguardian_platform
    ports:
      - "8000:8000"
    environment:
      - APP_NAME=MailGuardian AI
      - APP_ENV=production
      - DEBUG=False
      - VIRUSTOTAL_API_KEY=${VIRUSTOTAL_API_KEY:-}
      - ABUSEIPDB_API_KEY=${ABUSEIPDB_API_KEY:-}
      - ALIENVAULT_OTX_API_KEY=${ALIENVAULT_OTX_API_KEY:-}
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
    restart: unless-stopped
